# Tasks

- [ ] Add base event metadata requirements to docs.
- [ ] Add optional `NotifiableDomainEvent` contract or equivalent.
- [ ] Add `NotificationEventKey`, `NotificationAudienceHint` value objects/enums.
- [ ] Add normalized `NotificationIntent` in `platform.notification`.
- [ ] Add normalized `CommunicationIntent` in `platform.communication`.
- [ ] Implement typed rules for events that do not implement the optional contract.
- [ ] Update domain docs for event facts used by notification/communication.
