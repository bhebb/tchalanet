# Spec — Game/Pricing Promotion Support

Seed stable variants such as `MARYAJ_GRATUIT`. Temporary campaigns should not create one-off game codes; they use pricing snapshots with `pricingSource=PROMOTION`.
