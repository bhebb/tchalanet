# Spec — Promotion Evaluation Flow

`EvaluatePromotionQuery` supports `SALE_PREVIEW` and `SALE_CONFIRMATION`. Preview is non-binding. Confirmation may consume quota. The response is `PromotionDecision`.
