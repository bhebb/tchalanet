package com.tchalanet.server.core.promotion.api.model;

/**
 * Simple stacking policy enumeration for promotions.
 * Values are intentionally minimal; expand as needed by business rules.
 */
public enum PromotionStackingPolicy {
    STACKABLE,
    EXCLUSIVE
}

