package com.tchalanet.server.core.terminal.domain.model;

/** Physical hardware vs virtual terminal (web/mobile). Aligned with V100 CHECK constraint. */
public enum TerminalKind {
  PHYSICAL,
  VIRTUAL
}
