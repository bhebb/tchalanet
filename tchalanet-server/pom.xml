<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.tchalanet</groupId>
  <artifactId>tchalanet-server</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>tchalanet-server</name>
  <description>Tchalanet backend — multi-module aggregator</description>

  <modules>
    <module>tchalanet-common</module>
    <module>tchalanet-catalog</module>
    <module>tchalanet-platform</module>
    <module>tchalanet-core</module>
    <module>tchalanet-features</module>
    <module>tchalanet-app</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>25</java.version>

    <!-- Library versions -->
    <archunit.version>1.4.2</archunit.version>
    <caffeine.version>3.2.4</caffeine.version>
    <firebase-admin.version>9.10.0</firebase-admin.version>
    <json-schema-validator.version>3.0.6</json-schema-validator.version>
    <lombok.version>1.18.46</lombok.version>
    <mapstruct.version>1.6.3</mapstruct.version>
    <mockito.version>5.23.0</mockito.version>
    <pdfbox.version>3.0.8</pdfbox.version>
    <querydsl.version>5.1.0</querydsl.version>
    <shedlock.version>7.7.0</shedlock.version>
    <springdoc.version>3.0.3</springdoc.version>
    <testcontainers.version>1.21.4</testcontainers.version>
    <zxing.version>3.5.4</zxing.version>
    <rome.version>2.1.0</rome.version>
    <jackson.version>3.1.2</jackson.version>
    <hibernate.version>7.3.2.Final</hibernate.version>
    <!-- Spring Modulith -->
    <spring-modulith.version>2.1.0</spring-modulith.version>
    <jakarta.servlet-api.version>6.1.0</jakarta.servlet-api.version>
    <!-- Build plugins -->
    <maven.compiler.plugin.version>3.15.0</maven.compiler.plugin.version>
    <maven.enforcer.plugin.version>3.6.2</maven.enforcer.plugin.version>
    <maven.surefire.plugin.version>3.5.6</maven.surefire.plugin.version>
    <maven.failsafe.plugin.version>3.5.5</maven.failsafe.plugin.version>
    <jacoco.plugin.version>0.8.15</jacoco.plugin.version>
    <jacocoArgLine/>
    <jacoco.minimum.line.coverage>0.20</jacoco.minimum.line.coverage>
    <jacoco.halt.on.failure>false</jacoco.halt.on.failure>
    <spotless.plugin.version>3.8.0</spotless.plugin.version>
    <checkstyle.plugin.version>3.6.0</checkstyle.plugin.version>
    <checkstyle.config>checkstyle/checkstyle.xml</checkstyle.config>
    <pmd.plugin.version>3.28.0</pmd.plugin.version>
    <pmd.version>7.26.0</pmd.version>
    <spotbugs.plugin.version>4.10.3.0</spotbugs.plugin.version>
    <owasp.plugin.version>12.2.2</owasp.plugin.version>
    <sonar.maven.plugin.version>5.3.0.6276</sonar.maven.plugin.version>

    <sonar.projectKey>tchalanet:tchalanet-server</sonar.projectKey>
    <sonar.projectName>tchalanet-server</sonar.projectName>
    <sonar.host.url>https://sonar.yourcompany.tld</sonar.host.url>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Spring Modulith -->
      <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-starter-core</artifactId>
        <version>${spring-modulith.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-api</artifactId>
        <version>${spring-modulith.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-test</artifactId>
        <version>${spring-modulith.version}</version>
        <scope>test</scope>
      </dependency>
      <!-- Inter-module dependencies -->
      <dependency>
        <groupId>com.tchalanet</groupId>
        <artifactId>tchalanet-common</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.tchalanet</groupId>
        <artifactId>tchalanet-catalog</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.tchalanet</groupId>
        <artifactId>tchalanet-platform</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.tchalanet</groupId>
        <artifactId>tchalanet-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.tchalanet</groupId>
        <artifactId>tchalanet-features</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- Jackson 3.0 (v3 tools artifacts) -->
      <dependency>
        <groupId>tools.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>tools.jackson.core</groupId>
        <artifactId>jackson-core</artifactId>
        <version>${jackson.version}</version>
      </dependency>

      <!-- Mapping & Code Gen -->
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>${lombok.version}</version>
        <optional>true</optional>
      </dependency>
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct</artifactId>
        <version>${mapstruct.version}</version>
      </dependency>

      <!-- Utils -->
      <dependency>
        <groupId>com.github.ben-manes.caffeine</groupId>
        <artifactId>caffeine</artifactId>
        <version>${caffeine.version}</version>
      </dependency>
      <dependency>
        <groupId>com.rometools</groupId>
        <artifactId>rome</artifactId>
        <version>${rome.version}</version>
      </dependency>
      <!-- API & Documentation -->
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc.version}</version>
      </dependency>

      <!-- QueryDSL (Jakarta compatible version) -->
      <dependency>
        <groupId>com.querydsl</groupId>
        <artifactId>querydsl-jpa</artifactId>
        <version>${querydsl.version}</version>
        <classifier>jakarta</classifier>
      </dependency>
      <dependency>
        <groupId>com.querydsl</groupId>
        <artifactId>querydsl-apt</artifactId>
        <version>${querydsl.version}</version>
        <classifier>jakarta</classifier>
        <scope>provided</scope>
      </dependency>

      <!-- Distributed Locking -->
      <dependency>
        <groupId>net.javacrumbs.shedlock</groupId>
        <artifactId>shedlock-spring</artifactId>
        <version>${shedlock.version}</version>
      </dependency>
      <dependency>
        <groupId>net.javacrumbs.shedlock</groupId>
        <artifactId>shedlock-provider-jdbc-template</artifactId>
        <version>${shedlock.version}</version>
      </dependency>

      <dependency>
        <groupId>com.google.firebase</groupId>
        <artifactId>firebase-admin</artifactId>
        <version>${firebase-admin.version}</version>
      </dependency>

      <!-- Barcode & PDF -->
      <dependency>
        <groupId>com.google.zxing</groupId>
        <artifactId>core</artifactId>
        <version>${zxing.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.zxing</groupId>
        <artifactId>javase</artifactId>
        <version>${zxing.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.pdfbox</groupId>
        <artifactId>pdfbox</artifactId>
        <version>${pdfbox.version}</version>
      </dependency>

      <!-- JSON Schema (v3 for Jackson 3 support) -->
      <dependency>
        <groupId>com.networknt</groupId>
        <artifactId>json-schema-validator</artifactId>
        <version>${json-schema-validator.version}</version>
      </dependency>
<!--      jakarta servlet api -->
      <dependency>
        <groupId>jakarta.servlet</groupId>
        <artifactId>jakarta.servlet-api</artifactId>
        <version>${jakarta.servlet-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <!-- Testing -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers</artifactId>
        <version>${testcontainers.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>postgresql</artifactId>
        <version>${testcontainers.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${testcontainers.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>com.tngtech.archunit</groupId>
        <artifactId>archunit-junit5</artifactId>
        <version>${archunit.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${mockito.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- Rate limiting -->
      <dependency>
        <groupId>com.bucket4j</groupId>
        <artifactId>bucket4j-core</artifactId>
        <version>8.10.1</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
     <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <release>${java.version}</release>
          <parameters>true</parameters>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
            <path>
              <groupId>org.mapstruct</groupId>
              <artifactId>mapstruct-processor</artifactId>
              <version>${mapstruct.version}</version>
            </path>
            <path>
              <groupId>com.querydsl</groupId>
              <artifactId>querydsl-apt</artifactId>
              <version>${querydsl.version}</version>
              <classifier>jakarta</classifier>
            </path>
            <path>
              <groupId>jakarta.persistence</groupId>
              <artifactId>jakarta.persistence-api</artifactId>
              <version>3.2.0</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven.enforcer.plugin.version}</version>
        <executions>
          <execution>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion><version>[${java.version},)</version></requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${maven.surefire.plugin.version}</version>
        <configuration>
          <argLine>
            @{jacocoArgLine}
            -javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar
            --enable-native-access=ALL-UNNAMED
            -XX:+EnableDynamicAgentLoading
          </argLine>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.plugin.version}</version>
        <configuration>
          <java>
            <googleJavaFormat/>
          </java>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>${checkstyle.plugin.version}</version>
        <configuration>
          <configLocation>${checkstyle.config}</configLocation>
          <suppressionsLocation>checkstyle/suppressions.xml</suppressionsLocation>
          <failsOnError>false</failsOnError>
          <violationSeverity>warning</violationSeverity>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>${pmd.plugin.version}</version>
        <dependencies>
          <!-- When overriding the PMD version, every PMD artifact must move together:
               pmd-java ${pmd.version} calls pmd-core APIs absent from the plugin's
               bundled core (NoSuchMethodError otherwise). -->
          <dependency>
            <groupId>net.sourceforge.pmd</groupId>
            <artifactId>pmd-core</artifactId>
            <version>${pmd.version}</version>
          </dependency>
          <dependency>
            <groupId>net.sourceforge.pmd</groupId>
            <artifactId>pmd-java</artifactId>
            <version>${pmd.version}</version>
          </dependency>
        </dependencies>
        <configuration>
          <rulesets>
            <ruleset>pmd/ruleset.xml</ruleset>
          </rulesets>
          <!-- Correctness rules (priority 1-4) block the build; complexity
               advisories are pinned to priority 5 in the ruleset and only
               reported. Keep the codebase clean on 1-4 so `pmd:check` gates PRs. -->
          <failOnViolation>true</failOnViolation>
          <failurePriority>4</failurePriority>
          <printFailingErrors>true</printFailingErrors>
          <targetJdk>21</targetJdk>
          <excludeRoots>
            <excludeRoot>target/generated-sources</excludeRoot>
          </excludeRoots>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.plugin.version}</version>
        <configuration>
          <effort>Default</effort>
          <threshold>Medium</threshold>
          <failOnError>false</failOnError>
          <excludeFilterFile>spotbugs/exclude.xml</excludeFilterFile>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.owasp</groupId>
        <artifactId>dependency-check-maven</artifactId>
        <version>${owasp.plugin.version}</version>
        <configuration>
          <nvdApiKey>${env.NVD_API_KEY}</nvdApiKey>
          <!-- Fail on High/Critical (CVSS >= 7) so the score is meaningful.
               The CI OWASP job stays continue-on-error; this gates direct runs
               (`mvn dependency-check:aggregate`) and surfaces real exposure. -->
          <failBuildOnCVSS>7</failBuildOnCVSS>
          <suppressionFiles>
            <suppressionFile>dependency-check-suppressions.xml</suppressionFile>
          </suppressionFiles>
          <formats>
            <format>HTML</format>
            <format>JSON</format>
          </formats>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.plugin.version}</version>
        <configuration>
          <excludes>
            <!-- App bootstrap / Spring config -->
            <exclude>**/*Application.*</exclude>
            <exclude>**/*AutoConfiguration.*</exclude>
            <exclude>**/*Config.*</exclude>
            <exclude>**/*Configuration.*</exclude>
            <exclude>**/*Constants.*</exclude>
            <exclude>**/*NativeHints.*</exclude>
            <exclude>**/*OpenApi.*</exclude>
            <exclude>**/*Properties.*</exclude>

            <!-- CQRS data carriers (records, no logic) -->
            <exclude>**/*Command.*</exclude>
            <exclude>**/*Query.*</exclude>
            <exclude>**/*Result.*</exclude>
            <exclude>**/*Event.*</exclude>
            <exclude>**/*Dto.*</exclude>

            <!-- API models (public records / enums / value objects) -->
            <exclude>**/api/model/**</exclude>
            <exclude>**/api/command/**</exclude>
            <exclude>**/api/query/**</exclude>
            <exclude>**/api/event/**</exclude>

            <!-- Web layer models -->
            <exclude>**/*Request.*</exclude>
            <exclude>**/*Response.*</exclude>
            <exclude>**/*View.*</exclude>
            <exclude>**/*Projection.*</exclude>

            <!-- Infra / persistence -->
            <exclude>**/*JpaEntity.*</exclude>
            <exclude>**/*JpaRepository.*</exclude>
            <exclude>**/*SpringRepository.*</exclude>
            <exclude>**/*Controller.*</exclude>
            <exclude>**/*Adapter.*</exclude>
            <exclude>**/*Port.*</exclude>
            <exclude>**/*Mapper.*</exclude>
            <exclude>**/*MapperImpl.*</exclude>

            <!-- Generated / QueryDSL -->
            <exclude>**/Q*.*</exclude>

            <!-- Domain exceptions (no logic) -->
            <exclude>**/*Exception.*</exclude>
          </excludes>
        </configuration>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <configuration>
              <propertyName>jacocoArgLine</propertyName>
            </configuration>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <haltOnFailure>${jacoco.halt.on.failure}</haltOnFailure>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.minimum.line.coverage}</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
