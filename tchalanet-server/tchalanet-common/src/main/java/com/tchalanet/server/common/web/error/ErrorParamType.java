package com.tchalanet.server.common.web.error;

/** Public, serialization-safe value shapes supported by error and notice parameters. */
public enum ErrorParamType {
  STRING,
  INTEGER,
  DECIMAL,
  BOOLEAN,
  DATE,
  DATE_TIME,
  DISPLAY_LABEL
}
