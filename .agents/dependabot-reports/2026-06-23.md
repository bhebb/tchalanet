# Dependabot Review — 2026-06-23

## ✅ Merged
_None this run._

## ⚠️ Needs human review
- #178 `@angular-devkit/schematics` 21.2.13 → 21.2.16 (patch/dev) — CI unknown (no check runs registered); safe bump but cannot auto-merge without green CI
- #174 ngrx group (`@ngrx/effects`, `@ngrx/router-store`, `@ngrx/store`, `@ngrx/store-devtools`) 21.1.0 → 21.1.1 (patch) — CI unknown (no check runs registered); bug-fixes only, no breaking changes; safe to merge once CI is confirmed green

## 🔴 On hold
- #177 `@angular/forms` 21.2.15 → 22.0.2 — **major version bump** (Angular 21 → 22); requires human approval
- #176 `jsdom` 27.4.0 → 29.1.1 — **two major version jumps** (27 → 29); requires human approval
- #175 `@angular/language-service` 21.2.15 → 22.0.2 — **major version bump** (Angular 21 → 22); requires human approval
- #173 `jackson.version` 3.1.2 → 3.2.0 (minor, tchalanet-server) — CI **red** (build failure since 2026-06-20); do not merge until CI is green
- #171 `spring-boot-starter-parent` 4.0.6 → 4.1.0 (minor, tchalanet-server) — CI **red** (build failure since 2026-06-20); do not merge until CI is green

## Notes
- 0 PRs merged this run (no SAFE candidates — CI unknown on web patches, CI red on server minors, major bumps on hold).
- PRs #173 and #171 (server) have been failing CI since 2026-06-20 — the build failure predates this review and likely indicates a pre-existing server build issue unrelated to the dependency bumps themselves. Worth investigating independently.
- PRs #177, #175 (Angular 22) and #176 (jsdom 29) represent a coordinated major-version upgrade wave for the web slice; should be tackled together under a dedicated OpenSpec change.
- CI checks are not running on the web-slice Dependabot PRs (#178, #174) — worth verifying that GitHub Actions triggers are configured for `dependabot/` branches.
