# Dependabot Review — 2026-07-07

## ✅ Merged

_None — CI status is unknown for all open PRs; safety rules block all merges._

## ⚠️ Needs human review

- #224 chore(deps): bump the angular-minors group across 1 directory with 19 updates — All are patch bumps (Angular 22.0.4→22.0.5, NgRx 21.1.0→21.1.1). Would be SAFE but CI is unknown (no check runs recorded).
- #225 chore(deps-dev): bump @nx/eslint-plugin from 23.1.0-beta.4 to 23.1.0-pr.36127.8b9019b — Pre-release to PR-build version (`pr.36127.8b9019b` is a Nx CI artefact, not a stable release). CI unknown.
- #226 chore(deps-dev): bump @nx/playwright from 23.1.0-beta.4 to 23.1.0-pr.36127.8b9019b — Same Nx PR-build concern as #225. CI unknown.

## 🔴 On hold

- #227 chore(deps-dev): bump eslint from 9.39.4 to 10.6.0 — **Major version bump** (9→10). Requires human approval before merging.

## Notes

- All 4 open PRs have zero CI check runs (`total_count: 0`). This may indicate CI is not configured for Dependabot branches, or runs have not been triggered yet. Recommend checking CI configuration and re-running checks before merging #224.
- PRs #225 and #226 target a Nx PR-build pre-release identifier (`23.1.0-pr.36127.8b9019b`) rather than a stable or beta release. These are likely internal Nx CI builds and should not be merged until a proper stable/beta release is available.
- PRs #225 and #226 also interact with the linter configuration (`@nx/eslint-plugin` + `@nx/playwright`) at a time when #227 proposes a major ESLint upgrade — consider reviewing all three together.
- 0 PRs merged this run (limit is 5 per run).
