## 1. Analysis

- [ ] Compare `DOMAIN_USER.md`, `UserStatus`, and `V100__create_core_tables.sql` status semantics.
- [ ] Decide first-login bootstrap status and approval behavior.
- [ ] Decide the architectural boundary for method security on command handlers.
- [ ] Decide whether `@RequiresPermission` annotation should be deleted or retained as a compile-time marker.

## 2. Follow-up

- [ ] Update the canonical docs/specs with the selected policy.
- [ ] Create implementation tasks for any resulting code or migration changes.
