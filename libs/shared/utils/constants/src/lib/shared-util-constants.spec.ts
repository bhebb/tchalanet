import { sharedUtilConstants } from './shared-util-constants';

describe('sharedUtilConstants', () => {
  it('should work', () => {
    expect(sharedUtilConstants()).toEqual('shared-util-constants');
  });
});
