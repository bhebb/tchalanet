name: Web CI

on:
  push:
    branches: [ '**' ]
    paths:
      - 'apps/**'
      - 'libs/**'
      - 'nx.json'
      - 'project.json'
      - 'workspace.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'eslint.config.mjs'
      - '.stylelintrc.cjs'
      - 'tsconfig.json'
      - 'tsconfig.*.json'
  pull_request:
    branches: [ main, prod ]
    paths:
      - 'apps/**'
      - 'libs/**'
      - 'nx.json'
      - 'project.json'
      - 'workspace.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'eslint.config.mjs'
      - '.stylelintrc.cjs'
      - 'tsconfig.json'
      - 'tsconfig.*.json'
jobs:
  web_lint_build_test:
    runs-on: ubuntu-latest
    name: Lint & Build & Test Web/Mobile
    strategy:
      matrix:
        node-version: [ 20 ]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: use Node ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache Nx
        uses: actions/cache@v4
        with:
          path: .nx/cache
          key: nx-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            nx-${{ runner.os }}-

      - name: Sets the base and head SHAs for the nx affected commands in CI
        uses: nrwl/nx-set-shas@v4

      # (optionnel) scan sécu + orthographe, commente si tu veux aller au plus vite
      # - name: Security & spell scan
      #   run: pnpm scan

      - name: ESLint (affected)
        run: pnpm exec nx affected --target=lint --base=$NX_BASE --head=$NX_HEAD --exclude=tchalanet-server --parallel=3

      - name: Stylelint
        run: pnpm lint:styles

      - name: Typecheck (affected)
        run: pnpm exec nx affected -t=typecheck --base=$NX_BASE --head=$NX_HEAD --exclude=tchalanet-server --parallel=3 || true

      - name: Build Web
        run: pnpm exec nx build tchalanet-web

      - name: Build Mobile
        run: pnpm exec nx build tchalanet-mobile

      - name: E2E Web (affected)
        run: pnpm exec nx affected -t=e2e --base=$NX_BASE --head=$NX_HEAD --exclude=tchalanet-server --parallel=1 || true
     
