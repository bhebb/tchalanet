---
description: Turn an idea into an implementable spec (no code)
---

Canonical skill: `.agents/skills/spec-scoping/SKILL.md`
Related: `.agents/skills/openspec-workflow/SKILL.md`

No code. Identify impacted projects/modules, source docs, task breakdown (one slice each), risks, open questions, assumptions. Recommend whether an OpenSpec change is required.

Load only: root `AGENTS.md`, the routers needed to scope impact, named docs. Do not load implementations.
