# ADR Index

The canonical ADR folder is `tchalanet-docs/docs/03-adr/`.

Current published ADRs:

No published ADR pages beyond the folder index are currently present.

Legacy archived ADR files are not kept as a parallel source of truth. Recover a
decision into a current ADR only when it is still valid.
