# OpenSpec Archive Policy

OpenSpec archives keep implementation history discoverable without loading it
as active context.

Policy:

- Archive completed changes in the owning OpenSpec workspace.
- Keep global archives for cross-project changes only.
- Do not copy component archives into global OpenSpec.
- Do not delete archived proposals or specs during an inventory pass.
- Use a follow-up reviewed cleanup change before permanent deletion.
