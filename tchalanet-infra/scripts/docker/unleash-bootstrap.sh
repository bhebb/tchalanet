#!/usr/bin/env bash
# File removed as per project decision: bootstrap and edge automation deleted for v1.
# The previous contents (automatic token creation and seeding) were intentionally removed.
# If you need to run these tasks manually, implement `scripts/docker/unleash-bootstrap.sh` again
# or use the Unleash Admin API with a PAT.
exit 0
