# Infra Decisions

- P0 strict = Traefik + PostgreSQL.
- P0 plus = Traefik + PostgreSQL + Redis.
- server-v0 = Traefik + PostgreSQL + Redis + API + Edge + Web.
- Auth = Firebase external provider; Firebase Auth Emulator for local IDE.
