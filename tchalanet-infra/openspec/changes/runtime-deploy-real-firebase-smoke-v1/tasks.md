# Tasks

- [x] Reproduce the failed staging runtime deploy from run `31064483374`.
- [x] Fix intentional Firebase emulator-validator skips under `set -e`.
- [x] Add and run a regression test for the real-Firebase deploy path.
- [ ] Run infra validation and confirm a successful staging runtime deploy.
