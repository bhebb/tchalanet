# Traefik

Traefik route les services applicatifs :

- API
- web
- edge-service
- outils internes optionnels

Firebase est externe à la stack standard. Firebase Auth Emulator est exposé par
son compose dédié en local, pas par un routeur d'auth production.
