# envs/common/postgres.env - PostgreSQL runtime defaults (NON-SECRETS)
# PostgreSQL common config
POSTGRES_USER=postgres
POSTGRES_DB=postgres
POSTGRES_INITDB_ARGS=--data-checksums
POSTGRES_HOST_AUTH_METHOD=scram-sha-256
POSTGRES_CONTAINER=tchalanet-postgres
POSTGRES_VOLUME=pgdata

# Postgres tuning defaults (can be overridden in envs/<env>/compose.env)
PG_SHARED_BUFFERS=256MB
PG_WORK_MEM=16MB
PG_MAINTENANCE_WORK_MEM=128MB
PG_EFFECTIVE_CACHE_SIZE=1GB
PG_MEMORY_LIMIT=2G
PG_MEMORY_RESERVATION=1G
PG_LOG_MAX_SIZE=10m
PG_LOG_MAX_FILES=3

# Database names & users (non-secret defaults)
APP_DB_NAME=tchalanet_db
APP_DB_USER=app_user
UNLEASH_DB_NAME=unleash_db
UNLEASH_DB_USER=unleash_user
