# ==================================
# Infrastructure Configuration
# ==================================
# envs/prod/.env - runtime-only overrides (NON-SECRETS)

# Monitoring (runtime flags)
OTEL_ENABLED=true
PROMETHEUS_ENABLED=true
