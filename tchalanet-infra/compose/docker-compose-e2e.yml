services:
  api:
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:?SPRING_PROFILES_ACTIVE is required for E2E}
      TCH_IDENTITY_PROVIDER: ${TCH_IDENTITY_PROVIDER:?TCH_IDENTITY_PROVIDER is required for E2E}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:?FIREBASE_PROJECT_ID is required for E2E}
      FIREBASE_AUTH_EMULATOR_HOST: ${FIREBASE_AUTH_EMULATOR_HOST:?FIREBASE_AUTH_EMULATOR_HOST is required for E2E}
      FIREBASE_BOOTSTRAP_ENABLED: ${FIREBASE_BOOTSTRAP_ENABLED:?FIREBASE_BOOTSTRAP_ENABLED is required for E2E}
      FIREBASE_BOOTSTRAP_AUTO_RUN_ON_STARTUP: ${FIREBASE_BOOTSTRAP_AUTO_RUN_ON_STARTUP:?FIREBASE_BOOTSTRAP_AUTO_RUN_ON_STARTUP is required for E2E}
      TCH_SECURITY_USER_BOOTSTRAP_MODE: ${TCH_SECURITY_USER_BOOTSTRAP_MODE:?TCH_SECURITY_USER_BOOTSTRAP_MODE is required for E2E}
      TCH_IDENTITY_FIREBASE_BOOTSTRAP_USERS: ${TCH_IDENTITY_FIREBASE_BOOTSTRAP_USERS:?TCH_IDENTITY_FIREBASE_BOOTSTRAP_USERS is required for E2E}
